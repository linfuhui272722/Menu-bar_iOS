// SPDX-License-Identifier: MIT
// AirPortExtra — ported from macOS AirPort.menu (NSPrincipalClass=AirPortExtra,
// com.apple.menuextra.airport, NSMenuExtraWidth=22, version 11.0).
// On macOS the icon is drawn programmatically (signal arcs), not a static PDF.
#import "MBCore.h"

@interface AirPortExtra : NSMenuExtra
@end
